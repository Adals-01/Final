export default function Beeritem(props) {
  //   const beerArr = props.data.map((beerlist, index) => <Beerlistitem key={index} {...beerlist} />);
  console.log(props);
  console.log(props.thisBeer);
  return (
    <div className="Home">
      <main>
        <h1>hello</h1>
      </main>
    </div>
  );
}
